import { EndpointV2, Logger } from "@smithy/types";
import { EndpointParameters } from "@aws-sdk/client-sts/dist-types/ts3.4/endpoint/EndpointParameters";
export declare const defaultEndpointResolver: (
  endpointParams: EndpointParameters,
  context?: {
    logger?: Logger;
  }
) => EndpointV2;
