import { Logger } from "@smithy/types";
import { AwsCredentialIdentity } from "@aws-sdk/types/dist-types/ts3.4/identity";
import { Provider } from "@aws-sdk/types/dist-types/ts3.4/util";
export interface Credentials extends AwsCredentialIdentity {}
export type CredentialProvider = Provider<Credentials>;
export type CredentialProviderOptions = {
  logger?: Logger;
  parentClientConfig?: {
    region?: string | Provider<string>;
    [key: string]: unknown;
  };
};
