import {
  CognitoIdentityClient,
  CognitoIdentityClientConfig,
} from "@aws-sdk/credential-provider-cognito-identity/dist-types/ts3.4/loadCognitoIdentity";
import { Logins } from "@aws-sdk/credential-provider-cognito-identity/dist-types/ts3.4/Logins";
export interface CognitoProviderParameters {
  client?: CognitoIdentityClient;
  clientConfig?: CognitoIdentityClientConfig;
  customRoleArn?: string;
  logins?: Logins;
}
