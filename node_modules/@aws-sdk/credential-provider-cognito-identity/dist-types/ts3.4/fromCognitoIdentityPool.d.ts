import { CredentialProviderOptions } from "@aws-sdk/types";
import { CognitoProviderParameters } from "@aws-sdk/credential-provider-cognito-identity/dist-types/ts3.4/CognitoProviderParameters";
import { CognitoIdentityCredentialProvider } from "@aws-sdk/credential-provider-cognito-identity/dist-types/ts3.4/fromCognitoIdentity";
import { Storage } from "@aws-sdk/credential-provider-cognito-identity/dist-types/ts3.4/Storage";
export declare function fromCognitoIdentityPool({
  accountId,
  cache,
  client,
  clientConfig,
  customRoleArn,
  identityPoolId,
  logins,
  userIdentifier,
  logger,
  parentClientConfig,
}: FromCognitoIdentityPoolParameters): CognitoIdentityCredentialProvider;
export interface FromCognitoIdentityPoolParameters
  extends CognitoProviderParameters,
    CredentialProviderOptions {
  accountId?: string;
  cache?: Storage;
  identityPoolId: string;
  userIdentifier?: string;
}
