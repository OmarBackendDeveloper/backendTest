import { Logger } from "@smithy/types";
import { SsoProfile } from "@aws-sdk/credential-provider-sso/dist-types/ts3.4/types";
export declare const validateSsoProfile: (
  profile: Partial<SsoProfile>,
  logger?: Logger
) => SsoProfile;
