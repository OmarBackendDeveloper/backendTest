import { Logger } from "@smithy/types";
import { SsoProfile } from "@aws-sdk/credential-provider-sso/dist-types/types";
/**
 * @internal
 */
export declare const validateSsoProfile: (profile: Partial<SsoProfile>, logger?: Logger) => SsoProfile;
